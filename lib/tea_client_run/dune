(library
 (name tea_client_run)
 (modes byte)
 ; tea_rpc for the ONE definition of the delivery key's wire spelling
 ; (key_header, Key.to_string): the runtime keys the header the server parses,
 ; from the same module, so a one-byte drift is a compile error rather than a
 ; silent fall back to at-least-once (T3/T20).
 (libraries tea_client tea_rpc vdom ojs))
